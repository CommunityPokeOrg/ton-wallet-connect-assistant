[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ton-wallet-connect-assistant"
version = "0.1.0"
description = "Tonkeeper-style desktop TON wallet + headless Python SDK (TonConnect, keystore, jetton/NFT, sends)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "CommunityPoke" }]
dependencies = [
    "pytonconnect>=0.4.0,<0.5",
    "pytoniq>=0.1.40,<0.2",
    "pytoniq-core>=0.1.40,<0.3",
    "PyNaCl>=1.5,<2",
    "httpx>=0.27,<1",
    "qrcode[pil]>=7.4,<9",
]

[project.optional-dependencies]
gui = [
    "PySide6>=6.8,<6.12",
]
dev = [
    "pytest>=8,<9",
    "ruff>=0.5",
    "PySide6>=6.8,<6.12",
]

[project.scripts]
ton-wallet-assistant = "ton_wallet_assistant.__main__:run"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 120
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "ASYNC", "N"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ASYNC"]

[tool.pytest.ini_options]
testpaths = ["tests"]
